{{- $p := includeTemplate "platform.json" . | fromJson -}}

# ── Environment variables & PATH ─────────────────────────────────────────────

{{- if $p.isDarwin }}
export HOMEBREW_PREFIX="/opt/homebrew"
export HOMEBREW_BUNDLE_NO_UPGRADE=1
export DOCKER_HOST="unix://$HOME/.colima/default/docker.sock"

export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:$PATH"
export PATH="$PATH:$(go env GOPATH)/bin"
export PATH="$PATH:$HOME/.my-scripts/bin"

export EDITOR="nvim"
{{- else if $p.isLinux }}
export PATH="$PATH:$HOME/.local/bin"
export PATH="$PATH:$HOME/.cargo/bin"
export PATH="$PATH:$HOME/.my-scripts/bin"

gopath="$(go env GOPATH 2>/dev/null)" && export PATH="$PATH:$gopath/bin"

{{- if $p.isWSL }}
export EDITOR="nvim"
{{- else if $p.isNixOS }}
export EDITOR="hx"
{{- else }}
export EDITOR="nvim"
{{- end }}
{{- end }}

export LANG="en_US.UTF-8"
export LC_ALL="en_US.UTF-8"

{{- if $p.isWSL }}
# No GUI pinentry available in WSL -- GPG_TTY lets pinentry-curses prompt
# for the passphrase in the current terminal (needed for git commit signing).
export GPG_TTY="$(tty)"
{{- if index . "work_gpg_signing_key" }}
# gpg-agent otherwise stays pinned to whatever tty it first saw, so signing
# silently fails ("Operation cancelled") in any other/new terminal.
gpg-connect-agent updatestartuptty /bye >/dev/null 2>&1
{{- end }}
{{- end }}

{{- if and (not $p.isNixOS) (not $p.isWindows) }}
# Point neovim to the nvim/ subdirectory inside the cloned repo
export NVIM_APPNAME="neovim-config/nvim"
{{- end }}
