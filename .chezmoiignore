README.md
LICENSE

{{- $isLinux := eq .chezmoi.os "linux" }}
{{- $isWSL := and $isLinux (contains "microsoft" (lower .chezmoi.kernel.osrelease)) }}
{{- $isNixOS := and $isLinux (contains "nixos" (lower .chezmoi.hostname)) }}

# Zed runs on the Windows host, not inside WSL
{{ if $isWSL }}
.config/zed
{{ end }}

# macOS-only
{{ if ne .chezmoi.os "darwin" }}
.docker/config.json
.homebrew/
{{ end }}

# Windows only uses Zed — ignore everything else
{{ if eq .chezmoi.os "windows" }}
.zshrc
.zsh/
.alacritty.toml
.config/git
.config/starship.toml
.config/fastfetch
.config/helix
.config/lazygit
.config/zellij
.config/eza
.config/atuin
.my-scripts/
{{ end }}

# Windows-only
{{ if ne .chezmoi.os "windows" }}
AppData/
{{ end }}
